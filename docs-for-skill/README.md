# Docs for Skill 🚀

> **AI Skill Generator & Modular Document Ingestion Pipeline**  
> *Gerador de Skills para IA & Pipeline Modular de Ingestão de Documentos*

---

## 🇺🇸 English

### 📌 Overview
**Docs for Skill** is a production-grade, cross-platform toolchain designed to ingest large, complex source documents (PDFs, Word documents, Excel spreadsheets, PowerPoint decks, LibreOffice files, technical manuals, RPG rulebooks, legal codes) and transform them into **high-performance, token-efficient, modular AI Skills**.

Instead of dumping hundreds of pages directly into an AI model's context window—wasting tokens and degrading attention—**Docs for Skill** structures the knowledge base into segmented, on-demand chapters along with synthesized index files, cheatsheets, glossaries, and pattern guides.

---

### 🏗️ Architecture of a Generated Skill

Every skill generated by this pipeline adheres to a clean, standardized modular structure:

```text
my-generated-skill/
├── SKILL.md             # Master bilingual index (EN / PT-BR) & Domain Mental Model
├── cheatsheet.md        # Quick reference tables, formulas, immediate rules & commands
├── glossary.md          # Canonical dictionary of technical terms, entities & definitions
├── patterns.md          # Architecture rules, decision flows, best practices & mechanics
└── chapters/            # Cleaned, balanced chapters loaded on-demand (~15KB to 40KB each)
    ├── ch01-introduction.md
    ├── ch02-core-mechanics.md
    ├── ch03-entities-and-systems.md
    └── ...
```

---

### 🌐 Supported Formats & Conversion Pipeline

| Source Format | Native Parser / Extractor | Fallback & Preprocessing |
|---|---|---|
| **PDF (Digital & Scanned)** | `pdfminer.six` (Unicode text flow) | `pdfplumber` (tables) / `Tesseract` OCR |
| **Microsoft Word (`.docx`, `.doc`)** | `python-docx` / `mammoth` | `soffice` (LibreOffice) / `pandoc` |
| **Microsoft Excel (`.xlsx`, `.xls`)** | `openpyxl` (all worksheets) | `pandas` / `xlrd` |
| **Microsoft PowerPoint (`.pptx`, `.ppt`)** | `python-pptx` (visual order + speaker notes) | `soffice` / `pandoc` |
| **LibreOffice (`.odt`, `.ods`, `.odp`)** | `pandoc` / `odfpy` / `pandas` | `soffice --headless` |
| **Web & E-books (`.html`, `.epub`, `.rtf`)**| `markdownify` / `beautifulsoup4` / `ebooklib` | `striprtf` / `pandoc` |

---

### 🚀 Usage

#### 1. Environment Verification
Validate that all required Python packages and system utilities are available on your OS:
```bash
# Windows
python scripts/check_env.py

# Linux / macOS
python3 scripts/check_env.py
```

#### 2. Generate a Modular Skill
Convert and structure any document into a dedicated skill folder:
```bash
# Windows
python scripts/structure_skill.py "path\to\manual.pdf" "skill-name" [optional_output_dir]

# Linux / macOS
python3 scripts/structure_skill.py "path/to/manual.pdf" "skill-name" [optional_output_dir]
```

---

### 📦 Installation

```bash
# Complete Python dependencies
pip install markitdown pdfminer.six pdfplumber python-docx openpyxl Pillow python-pptx mammoth pandas odfpy xlrd striprtf extract-msg markdownify
```

**Optional System Tools:**
- **Pandoc:** `winget install JohnMacFarlane.Pandoc` (Windows) | `brew install pandoc` (macOS) | `sudo apt install pandoc` (Linux)
- **Tesseract OCR:** `winget install UB-Mannheim.TesseractOCR` (Windows) | `brew install tesseract` (macOS) | `sudo apt install tesseract-ocr` (Linux)

---
---

## 🇧🇷 Português (Brasil)

### 📌 Visão Geral
O **Docs for Skill** é um conjunto de ferramentas multiplataforma de nível de produção desenvolvido para ingerir documentos extensos e complexos (PDFs, documentos Word, planilhas Excel, apresentações PowerPoint, arquivos LibreOffice, manuais técnicos, livros de regras de RPG e legislações) e transformá-los em **Skills Modulares de Alto Desempenho e Máxima Eficiência de Tokens para Agentes de IA**.

Em vez de despejar centenas de páginas diretamente na janela de contexto da IA — o que desperdiça tokens e dilui a capacidade de raciocínio do modelo —, o **Docs for Skill** estrutura a base de conhecimento em capítulos segmentados e carregados sob demanda, acompanhados de um índice mestre, guia rápido, glossário e documento de padrões.

---

### 🏗️ Arquitetura de uma Skill Gerada

Cada skill gerada por este pipeline adota uma estrutura modular padronizada:

```text
minha-skill-gerada/
├── SKILL.md             # Índice mestre bilíngue (EN / PT-BR) e Modelo Mental do Domínio
├── cheatsheet.md        # Tabelas de consulta rápida, fórmulas, regras imediatas e comandos
├── glossary.md          # Dicionário canônico de termos técnicos, entidades e definições
├── patterns.md          # Padrões de arquitetura, fluxos de decisão, regras e mecânicas
└── chapters/            # Capítulos higienizados e carregados sob demanda (~15KB a 40KB cada)
    ├── ch01-introducao.md
    ├── ch02-mecanicas-centrais.md
    ├── ch03-entidades-e-sistemas.md
    └── ...
```

---

### 🌐 Formatos Suportados & Pipeline de Conversão

| Formato de Origem | Extrator / Parser Principal | Fallback & Pré-processamento |
|---|---|---|
| **PDF (Digital e Escaneado)** | `pdfminer.six` (preservação de texto e acentos) | `pdfplumber` (tabelas) / `Tesseract` OCR |
| **Microsoft Word (`.docx`, `.doc`)** | `python-docx` / `mammoth` | `soffice` (LibreOffice) / `pandoc` |
| **Microsoft Excel (`.xlsx`, `.xls`)** | `openpyxl` (todas as abas da planilha) | `pandas` / `xlrd` |
| **Microsoft PowerPoint (`.pptx`, `.ppt`)** | `python-pptx` (ordem visual + notas do orador) | `soffice` / `pandoc` |
| **LibreOffice (`.odt`, `.ods`, `.odp`)** | `pandoc` / `odfpy` / `pandas` | `soffice --headless` |
| **Web & E-books (`.html`, `.epub`, `.rtf`)**| `markdownify` / `beautifulsoup4` / `ebooklib` | `striprtf` / `pandoc` |

---

### 🚀 Modo de Uso

#### 1. Verificação do Ambiente
Valide se todos os pacotes Python e binários de sistema estão disponíveis no seu SO:
```bash
# Windows
python scripts/check_env.py

# Linux / macOS
python3 scripts/check_env.py
```

#### 2. Estruturar uma Nova Skill
Converta e estruture qualquer documento em uma pasta de skill dedicada:
```bash
# Windows
python scripts/structure_skill.py "caminho\para\documento.pdf" "nome-da-skill" [diretorio_saida_opcional]

# Linux / macOS
python3 scripts/structure_skill.py "caminho/para/documento.pdf" "nome-da-skill" [diretorio_saida_opcional]
```

---

### 📦 Instalação de Dependências

```bash
# Dependências completas em Python
pip install markitdown pdfminer.six pdfplumber python-docx openpyxl Pillow python-pptx mammoth pandas odfpy xlrd striprtf extract-msg markdownify
```

**Ferramentas Opcionais de Sistema:**
- **Pandoc:** `winget install JohnMacFarlane.Pandoc` (Windows) | `brew install pandoc` (macOS) | `sudo apt install pandoc` (Linux)
- **Tesseract OCR:** `winget install UB-Mannheim.TesseractOCR` (Windows) | `brew install tesseract` (macOS) | `sudo apt install tesseract-ocr` (Linux)

---

### 🛡️ Autonomous Error Recovery & Self-Healing Protocol (For AI Agents)

When an AI Agent encounters errors during skill installation, execution, or environment setup:
1. **Root Cause Diagnosis & Self-Correction:** The agent MUST analyze the underlying cause of failure (missing packages, permissions, OS binary gaps, charset issues) and iteratively attempt self-correction until the skill performs its intended purpose.
2. **Permission Gate for Extra / System Software:** If resolving an issue requires installing system binaries (pandoc, libreoffice, tesseract), elevated global packages, or configuring API keys, the agent MUST explicitly explain the situation to the developer/operator and request authorization before proceeding.
3. **Configuration Report & Verification:** Upon approval, the agent must clearly state what was installed and configured, then immediately execute an end-to-end test or ask the operator for sample inputs to validate that the skill is 100% operational.

---

### 🛡️ Protocolo de Auto-Correção e Recuperação de Erros (Para Agentes de IA)

Sempre que o Agente de IA encontrar falhas na instalação, execução ou configuração de ambiente da skill:
1. **Diagnóstico de Causa Raiz e Auto-Correção:** O agente DEVE analisar a causa exata do erro (dependências ausentes, permissões, ferramentas de SO, encoding de caracteres) e tentar se auto-corrigir iterativamente até que a skill esteja funcionando perfeitamente conforme seu propósito.
2. **Solicitação de Permissão para Instalações Extras:** Se a correção exigir a instalação de binários de sistema (pandoc, libreoffice, tesseract), pacotes globais ou configuração de chaves de API, o agente DEVE explicar o diagnóstico com clareza e solicitar a permissão explícita do desenvolvedor/operador antes de instalar.
3. **Relatório de Configuração e Validação:** Após a confirmação, o agente deve informar detalhadamente o que foi instalado e configurado para resolver o problema e executar imediatamente um teste de validação (ou solicitar ao operador os dados/arquivos necessários para testar a skill).

---

## 👨‍💻 Author / Autor

**Charles Corrêa**  
- 📧 **Email:** [charlescorreaweb@gmail.com](mailto:charlescorreaweb@gmail.com)  
- 📷 **Instagram:** [@mestrecharlescorrea](https://www.instagram.com/mestrecharlescorrea)  
- 🌐 **Websites:** [charlescorrea.com.br](https://charlescorrea.com.br) | [rpg.charlescorrea.com.br](https://rpg.charlescorrea.com.br)  
- 🐙 **GitHub:** [CharlesCorreaDev](https://github.com/CharlesCorreaDev)  
- ⚡ **Skills de IA By Charles Corrêa Repo:** [https://github.com/CharlesCorreaDev/skills-ai](https://github.com/CharlesCorreaDev/skills-ai)

